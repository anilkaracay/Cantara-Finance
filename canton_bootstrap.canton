// Connect participant to the domain
participant1.domains.connect_local(domains.local)

// Upload the DAR file
// The path must match the volume mount in docker-compose.yml
participant1.dars.upload("/canton/cantara-model.dar")

// Enable automatic party allocation if needed (optional, usually handled by app)
