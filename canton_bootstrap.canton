// Canton Bootstrap Script - Manual domain connection

// Wait for domain and participant to initialize
Thread.sleep(3000)

// Connect using domain alias (string) instead of domains.local reference
participant1.domains.connect("local")

println("Bootstrap completed - domain connected")
