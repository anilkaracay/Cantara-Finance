// Canton Bootstrap Script - Correct API usage

// Wait for domain and participant to initialize
Thread.sleep(3000)

// Get the local domain reference (first element from the list)
val localDomain = domains.local.head

// Connect participant to domain using the domain reference
participant1.domains.connect_local(localDomain)

// Upload the DAR file
participant1.dars.upload("/canton/cantara-model.dar")

println("Bootstrap completed - domain connected and DAR uploaded")
