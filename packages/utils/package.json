{
    "name": "@cantara/utils",
    "version": "0.0.0",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "lint": "eslint . --ext .ts",
        "check-types": "tsc --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@cantara/types": "*"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "eslint": "^8.0.0"
    }
}